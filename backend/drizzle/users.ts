import { pgTable, text, boolean, timestamp, doublePrecision, integer } from 'drizzle-orm/pg-core';
import { userStatusEnum } from './user-status-enum';

export const users = pgTable('users', { id: text('id').primaryKey(), phone: text('phone'), email: text('email'), login: text('login').notNull(), first_name: text('first_name'), last_name: text('last_name'), password: text('password').notNull(), salt: text('salt'), is_super_user: boolean('is_super_user').default(false).notNull(), status: userStatusEnum('status').notNull(), card_name: text('card_name'), card_number: text('card_number'), birth_date: timestamp('birth_date', { mode: 'date', precision: 3 }), car_model: text('car_model'), car_number: text('car_number'), is_online: boolean('is_online').default(false).notNull(), latitude: doublePrecision('latitude').default(0).notNull(), longitude: doublePrecision('longitude').default(0).notNull(), fcm_token: text('fcm_token'), wallet_balance: doublePrecision('wallet_balance').default(0).notNull(), max_active_order_count: integer('max_active_order_count'), doc_files: text('doc_files').array().notNull(), order_start_date: timestamp('order_start_date', { mode: 'date', precision: 3 }), app_version: text('app_version'), created_at: timestamp('created_at', { mode: 'date', precision: 3 }).defaultNow().notNull(), updated_at: timestamp('updated_at', { mode: 'date', precision: 3 }).defaultNow().notNull(), api_token: text('api_token'), tg_id: text('tg_id') });